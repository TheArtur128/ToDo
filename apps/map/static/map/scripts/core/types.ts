export type Map = {id: number}

export type Task = {
    id: number,
    description: string,
    x: number,
    y: number,
}

export type TaskPrototype = {
    description: string,
    x: number,
    y: number,
}
